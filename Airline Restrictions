#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
    int t,a,b,c,d,e,sum1,sum2,sum3;
    cin>>t;
    while(t--)
    {
        cin>>a>>b>>c>>d>>e;
      sum1=a+b;
      sum2=b+c;
      sum3=a+c;
      if((sum1<=d && c<=e) || (sum2<=d && a<=e) || (sum3<=d && b<=e))
      cout<<"YES"<<endl;
      else
      cout<<"NO"<<endl;
    }
}
